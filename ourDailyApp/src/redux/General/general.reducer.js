import GeneralActionTypes from "./general.types";

const INITIATE_STATE = {

};

const generalReducer = (state = INITIATE_STATE, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default themeRegeneralReducerducer;